/**
 * Build tooling expects a top-level `src/index.ts` entry. The SPFx web part
 * is registered from `lib/webparts/documentAi/DocumentAiWebPart.js` via the
 * solution manifest, not this file.
 */
export {};
